{
  "data_asset_type": "Dataset",
  "expectation_suite_name": "fact_pageviews_contract",
  "expectations": [
    {
      "expectation_type": "expect_table_columns_to_match_set",
      "kwargs": {
        "column_set": [
          "user_id",
          "event_type",
          "event_timestamp",
          "session_info",
          "country",
          "email_opt_in"
        ]
      },
      "meta": {
        "notes": "Checks that the final columns match the expected analytical table schema."
      }
    },
    {
      "expectation_type": "expect_column_values_to_be_in_set",
      "kwargs": {
        "column": "event_type",
        "value_set": ["page_view"]
      },
      "meta": { "notes": "Ensures only pageview events exist in this table." }
    },
    {
      "expectation_type": "expect_column_values_to_not_be_null",
      "kwargs": {
        "column": "country"
      },
      "meta": { "notes": "Ensures the join with the profiles table was successful." }
    }
  ],
  "meta": {
    "great_expectations_version": "0.18.9"
  }
}
